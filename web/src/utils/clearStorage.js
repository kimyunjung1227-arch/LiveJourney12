// localStorage 초기화 유틸리티
// 앱을 완전히 처음 설치한 상태로 되돌립니다

export const clearAllStorage = () => {
  // 서버 운영 전환: localStorage 사용 제거 → no-op
};

// 앱 시작시 초기화 (현재 비활성)

// 수동 초기화가 필요할 때만 사용
// 브라우저 콘솔에서: import('./utils/clearStorage').then(m => m.clearAllStorage())








